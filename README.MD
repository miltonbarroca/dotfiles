Apos clonar repo, rodar:

cd ~/dotfiles
./setup.sh

ativar o venv:
source venv/bin/activate

# Em scripts, o nome do arquivo sera o sym!

atualiza os submodulos:

git submodule update --remote --merge